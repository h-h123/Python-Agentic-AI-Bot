[database]
; Database configuration (SQLite in this case)
database_url = sqlite:///railway_reservation.db
echo = False

[logging]
; Logging configuration
log_level = INFO
log_file = railway_reservation.log

[system]
; System settings
max_seats_per_train = 100
default_train_capacity = 50
booking_expiry_days = 30

[api]
; API settings (if expanded to web service)
host = 127.0.0.1
port = 5000
debug = False